# Microgrid Agent System - Certificate Configuration
#
# List all agents that should have mTLS certificates generated.
# Format: one agent name per line, lowercase alphanumeric with hyphens/underscores
#
# To add a new agent:
#   1. Add agent name below
#   2. Run: ./generate_certs.sh generate
#   3. Update docker-compose.yml to mount /app/shared/certs
#   4. Update config.py in agent to load certificates
#   5. Add FastAPI HTTPS endpoint to agent
#
# Current agents (Phase 1):

# Core control and coordination
microgrid-agent
control-agent
researcher-agent
monitor-agent

# Additional agents (Phase 2+, uncomment as needed):
# solar-agent
# wind-agent
# battery-agent
# load-agent
